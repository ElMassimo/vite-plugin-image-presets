{
  "name": "vite-plugin-image-presets-example",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "cross-env DEBUG=image-presets:* vp dev --open",
    "build": "cross-env DEBUG=image-presets:* vp build --mode production",
    "preview": "cross-env RAILS_ENV=production DEBUG=image-presets:* vp preview --open",
    "now": "npm run build && npm run preview"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5",
    "cross-env": "^7.0.3",
    "typescript": "^5",
    "vite": "catalog:",
    "vite-plugin-image-presets": "workspace:*",
    "vite-plus": "catalog:",
    "vue": "^3.2.37"
  }
}
